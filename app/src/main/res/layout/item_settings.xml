<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="?attr/selectableItemBackground"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/item_settings_icon"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_marginHorizontal="18dp"
        android:src="@drawable/ic_nav_admin" />

    <ImageView
        android:id="@+id/item_settings_arrow"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_marginHorizontal="18dp"
        android:src="@drawable/ic_nav_right" />

    <TextView
        android:id="@+id/item_settings_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_toStartOf="@id/item_settings_arrow"
        android:gravity="center_vertical"
        android:text="SubTitle"
        android:textColor="@color/black"
        android:textSize="@dimen/text_size_16sp" />

    <TextView
        android:id="@+id/item_settings_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_toEndOf="@id/item_settings_icon"
        android:gravity="center_vertical"
        android:text="Title"
        android:textColor="@color/black"
        android:textSize="@dimen/text_size_16sp" />
</RelativeLayout>
